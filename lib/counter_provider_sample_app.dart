import 'package:flutter/material.dart';

class CounterProviderSampleApp extends StatelessWidget {
  const CounterProviderSampleApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
